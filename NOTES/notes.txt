data types in js
Number
64 bits
sign(1-neg,0-pos)
float

Boolean
String
undefined
null
Object
BigInt
Symbol

node is a runtime environment for js

resolve function is used to fulfil the promise

setTimeout is a built-in JavaScript function that schedules a
function to be executed after a specified delay in milliseconds (ms).

when the 2 sec completed resolve